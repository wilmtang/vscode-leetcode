name: VS Code Extension

on:
  pull_request:
    branches: [master]
    paths:
      - src/**
      - resources/**
      - package.json
      - package-lock.json
      - README.md
      - CHANGELOG.md
      - .vscodeignore
      - tsconfig.json
      - tslint.json
      - .github/workflows/vscode-extension.yml
  push:
    branches: [master]
    tags:
      - vscode-extension-v*
    paths:
      - src/**
      - resources/**
      - package.json
      - package-lock.json
      - README.md
      - CHANGELOG.md
      - .vscodeignore
      - tsconfig.json
      - tslint.json
      - .github/workflows/vscode-extension.yml
  workflow_dispatch:
    inputs:
      publish:
        description: Publish this version to the VS Code Marketplace
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

jobs:
  validate:
    name: Validate VSIX
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Package VSIX
        run: |
          mkdir -p dist
          npm run build -- --out dist/vscode-leetcode-auth-sync.vsix

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: vscode-extension-vsix
          path: dist/*.vsix
          if-no-files-found: error

  publish:
    name: Publish to VS Code Marketplace
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    if: startsWith(github.ref, 'refs/tags/vscode-extension-v') || github.event.inputs.publish == 'true'
    environment: vscode-marketplace
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify tag matches VS Code extension version
        if: startsWith(github.ref, 'refs/tags/vscode-extension-v')
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          TAG="${GITHUB_REF_NAME#vscode-extension-v}"
          test "$VERSION" = "$TAG" || {
            echo "Tag vscode-extension-v$TAG does not match package.json version $VERSION"
            exit 1
          }

      - name: Package VSIX
        run: |
          mkdir -p dist
          npm run build -- --out dist/vscode-leetcode-auth-sync.vsix

      - name: Publish VSIX
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npm run vs-publish -- --packagePath dist/vscode-leetcode-auth-sync.vsix
